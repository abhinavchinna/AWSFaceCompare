import os

class Settings:
    AWS_REGION = os.environ.get("AWS_REGION", "your-aws-region")  # Replace with your AWS region
    AWS_ACCESS_KEY_ID = os.environ.get("AWS_ACCESS_KEY_ID")
    AWS_SECRET_ACCESS_KEY = os.environ.get("AWS_SECRET_ACCESS_KEY")

settings = Settings()